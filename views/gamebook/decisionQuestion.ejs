<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>View for Scenario Questions</title>

    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
      integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk"
      crossorigin="anonymous"
    />

    <link rel="stylesheet" type="text/css" href="/css/style.css" />
  </head>

  <body>
    <div class="container">
      <div id="home" class="flex-center flex-column">
        <form
          class="add-answers-form"
          id="input"
          action="/gamebook/decisionQuestion/<%=participant_id %>/<%=question_id%>"
          method="POST"
        >
          <br />

          <div class="container">
            <hr />
            <h4 class="font-weight-light text-justify" id="conclusion">
              Considering your decision ...
            </h4>
            <hr />

            <p>Question ID: <%= question_id %> ****REMOVE THIS LATER</p>
            <br />
            <h5 class="text-justify">
              <label for="decisionQuestion"><%=decisionQuestion %></label>
            </h5>
            <select
              class="form-control form-control-lg"
              name="decisionQuestion"
              required
            >
              <option value="" disabled selected
                >Please select your answer ...</option
              >
              <option value="1"><%= cQuestion1 %> </option>
              <option value="2"><%= cQuestion2 %></option>
              <% if(cQuestion3) { %>
              <option value="3"><%= cQuestion3 %> </option>
              <% } %> <% if(cQuestion4) { %>
              <option value="4"><%= cQuestion4 %> </option>
              <% } %>
              <option value="0">None</option>
            </select>
            <div class="invalid-feedback">Please select an answer.</div>
            <br />
            <button class="btn" id="final_submit" type="submit" method="POST">
              CONTINUE
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
